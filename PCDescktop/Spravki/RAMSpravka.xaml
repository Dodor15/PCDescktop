<Window x:Class="PCDescktop.Spravki.RAMSpravka"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PCDescktop.Spravki"
        mc:Ignorable="d"
        Title="RAMSpravka" Height="450" Width="800">
    <Grid>
        <TextBlock>
            <Run>Выбранная вами оперативная память </Run>
            <Run Text="{Binding RAMName}" FontWeight="Bold" FontSize="12"/>
            <Run> Имеет данные характеристики: </Run>
            <LineBreak/>
            <Run Text="{Binding RAMsocket}" FontWeight="Bold" FontSize="12"/>
            <Run> - Сокет оперативной памяти</Run>
            <LineBreak/>
            <LineBreak/>
            <Run Text="{Binding RamMemoryCount}" FontWeight="Bold" FontSize="12"/>
            <Run> - Количество памяти в плашку, так как в матери может быть больше одного разъема, если вставить несколько плашек, то колличество памяти суммируется.</Run>
            <LineBreak/>
            <LineBreak/>
            <Run Text="{Binding RamMemorySpeed}" FontWeight="Bold" FontSize="12"/>
            <Run> Скорость работы видеопамяти</Run>
        </TextBlock>
    </Grid>
</Window>
